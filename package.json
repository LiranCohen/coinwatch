{
  "name": "coinwatch",
  "private": true,
  "type": "module",
  "workspaces": [
    "shared",
    "web",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@chainwatch/web",
    "build": "npm run build --workspace=@chainwatch/web"
  }
}
